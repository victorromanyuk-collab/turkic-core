# 🛠️ Инструкция по настройке turkic.core

## Шаг 1: Создание Xcode проекта

Поскольку `.xcodeproj` файлы бинарные и зависят от вашей системы, вам нужно создать проект вручную:

1. Откройте **Xcode 15+**
2. `File → New → Project`
3. Выберите **iOS → App**
4. Настройки:
   - **Product Name:** `TurkicCore`
   - **Team:** None (или ваш Apple ID)
   - **Organization Identifier:** `com.turkiccore`
   - **Interface:** SwiftUI
   - **Language:** Swift
   - **Storage:** SwiftData ← важно!
   - **Include Tests:** ✓
5. Сохраните в папку с клонированным репозиторием

## Шаг 2: Добавление исходных файлов

### Вариант A: Замена папки (рекомендуется)
1. Удалите созданную Xcode папку `TurkicCore/` (с шаблонными файлами)
2. Скопируйте папку `TurkicCore/` из репозитория на её место
3. В Xcode: правый клик на проект → "Add Files to TurkicCore"
4. Выберите папку TurkicCore целиком
5. ✓ "Create groups"
6. ✓ "Copy items if needed" (если файлы вне папки проекта)
7. Target: TurkicCore ✓

### Вариант B: Пошаговое добавление
1. Добавьте каждую папку отдельно:
   - Models/
   - Views/
   - Services/
   - Utilities/
   - Resources/

## Шаг 3: Настройка words.json

**КРИТИЧЕСКИ ВАЖНО:** файл `words.json` должен быть включён в бандл

1. Выберите `words.json` в навигаторе Xcode
2. В инспекторе справа проверьте:
   - **Target Membership:** ✓ TurkicCore
   - **Location:** Relative to Group

## Шаг 4: Настройка Build Settings (опционально)

Для избежания предупреждений:

1. Выберите проект в навигаторе
2. Build Settings → поиск "Swift"
3. **Swift Language Version:** 5.9

## Шаг 5: Запуск

1. Выберите симулятор (iPhone 15 рекомендуется)
2. Нажмите ▶️ или `⌘R`
3. Приложение должно запуститься с главным экраном

## Возможные проблемы

### "Could not load words.json"
- Убедитесь, что `words.json` добавлен в Target Membership
- Проверьте что файл в `TurkicCore/Resources/words.json`

### "Missing model container"
- Убедитесь, что при создании проекта выбрали **SwiftData**
- Или добавьте import SwiftData во все модели

### Ошибки компиляции
- Убедитесь, что iOS Deployment Target = 17.0
- Swift Language Version = 5.9

## GitHub Actions

После пуша на GitHub, Actions будет пытаться собрать проект.
Для успешной сборки нужен рабочий `.xcodeproj`.

**Рекомендация:** Создайте проект локально, затем добавьте `.xcodeproj` в репозиторий:

```bash
git add TurkicCore.xcodeproj/
git commit -m "Add Xcode project file"
git push
```

## Структура после настройки

```
turkic-core/
├── .github/workflows/ios.yml
├── .gitignore
├── .swiftlint.yml
├── LICENSE
├── README.md
├── SETUP.md
├── TurkicCore/
│   ├── TurkicCoreApp.swift
│   ├── Models/
│   ├── Views/
│   ├── Services/
│   ├── Utilities/
│   └── Resources/
│       └── words.json
└── TurkicCore.xcodeproj/     ← создаётся Xcode
    └── project.pbxproj
```
